specVersion: "v1"
serviceId: "memoapp"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "memoapp-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
